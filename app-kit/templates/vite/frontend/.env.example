# Populated automatically by CodePipeline's build env from the CDK stack outputs.
# For local dev, fill these in from `aws cloudformation describe-stacks` after the first deploy.
VITE_AWS_REGION=us-east-1
VITE_USER_POOL_ID=
VITE_USER_POOL_CLIENT_ID=
VITE_API_URL=
# Only set if the LLM Lambda block is enabled in the CDK stack:
# VITE_LLM_URL=
